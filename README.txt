EXECUTION INSTRUCTIONS:

Compile the main.c file by typing the following into the command line:

	gcc --std=gnu99 -o smallsh main.c

Then run the executable file movies by typing the following:

	./smallsh

